[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps --include=dev"]

[phases.build]
cmds = ["bash build.sh"]

[start]
cmd = "cd backend && node dist/main.js"
